<div class='span5'>
  <h2><%= t('devise.form.sign_in') %></h2>
  <div class='well'>
    <%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
      <%= f.input :email %>
      <%= f.input :password %>
      <%= f.submit t('devise.form.sign_in') %>
    <% end %>
  </div>
  <%= render "links" %>
</div>
